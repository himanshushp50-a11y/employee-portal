# Backend API ka URL. Local dev me isko khaali chhod sakte ho — code apne aap
# http://localhost:8000 use karega (see src/api/client.ts).
# Production (Vercel) me ye .env.production me set hai aur Vercel dashboard me bhi
# set kar sakte ho: Settings → Environment Variables → VITE_API_URL
VITE_API_URL=https://employee-portal-qw1o.onrender.com
